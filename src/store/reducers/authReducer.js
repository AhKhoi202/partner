const initState = {
    isLoggedIn: true,
    token: 123
}

const userReducers = (state = initState, action) => {
  switch (action.type) {

    default:
      return state;
  }
}

export default userReducers